# dependencies
node_modules/

# build output
dist/

# 4K source frame sets — build inputs, not web assets.
# Regenerate the shipped hero with `npm run encode:frames`.
_source-frames/

# source footage (kept locally, too large for git)
*.mp4
test_frame.webp
# ...but the encoded panel loops are web assets: the MP4 is the fallback for
# browsers without VP9, and without this it 404s in production.
!public/project-video/*.mp4

# editor / os / tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
.env
_incoming/
.playwright-mcp/
