{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "headers": [
    {
      "comment": "Hero frames, panel loops and posters have stable filenames and only change when the encode scripts are re-run. Vite already hashes /assets/*, but these live in public/ and would otherwise be revalidated on every visit — 180 frame requests per page load makes that expensive.",
      "source": "/(hero|project-video|posters)/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=604800, stale-while-revalidate=2592000"
        }
      ]
    },
    {
      "source": "/(favicon.svg|favicon-32.png|favicon-180.png|og-image.png)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=604800" }]
    }
  ]
}
